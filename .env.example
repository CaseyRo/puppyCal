# Optional: analytics (Umami). If set, script tag is injected at build time.
# UMAMI_WEBSITE_ID=
# UMAMI_SCRIPT_URL=/stats.php?file=script.js
# UMAMI_HOST_URL=/stats.php

# Notification webhook: POST { "email": "..." } when user submits email (non-blocking).
# Endpoint must allow this app's origin (CORS) or be same-origin.
# NOTIFICATION_WEBHOOK_URL=

# Legal pages (German company). Set to full URLs; links are omitted if empty.
# Your Datenschutz/Impressum pages must cover: optional email collection (purpose: notify when schedule runs out; data sent to operator) and analytics (e.g. Umami) if used.
# DATA_POLICY_URL=   (Datenschutz)
# PRIVACY_URL=
# IMPRESSUM_URL=
